server:
  port: 9001
spring:
  profiles:
    active: dev
  application:
    name: l-mix-cloud-auth
  cloud:
    nacos:
      discovery:
        server-addr: localhost
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  redis:
    database: 0
    port: 6379
    host: localhost
    password:
management:
  endpoints:
    web:
      exposure:
        include: "*"
