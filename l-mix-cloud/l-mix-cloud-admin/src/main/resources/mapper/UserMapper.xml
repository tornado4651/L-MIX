<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tornado4651.lmix.cloud.admin.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.tornado4651.lmix.cloud.admin.domain.User">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="username" column="username" jdbcType="VARCHAR"/>
            <result property="password" column="password" jdbcType="VARCHAR"/>
            <result property="nickName" column="nick_name" jdbcType="VARCHAR"/>
            <result property="phone" column="phone" jdbcType="VARCHAR"/>
            <result property="email" column="email" jdbcType="VARCHAR"/>
            <result property="status" column="status" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,username,password,
        nick_name,phone,email,
        status
    </sql>


    <select id="getLoginUserInfo" resultType="com.tornado4651.lmix.cloud.common.dto.UserDTO">
        select a_user.id, a_user.username,  a_user.password, a_user.status
        from a_user
        <where>
            <if test="username != null and username != ''">
                a_user.username = #{username}
            </if>
        </where>
    </select>
</mapper>
