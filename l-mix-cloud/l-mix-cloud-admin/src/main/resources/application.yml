server:
  port: 9003
spring:
  application:
    name: l-mix-cloud-admin
  cloud:
    nacos:
      discovery:
        server-addr: localhost
  datasource:
    url: jdbc:mysql://localhost:3306/lmix
    username: lmix
    password: lmix
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl